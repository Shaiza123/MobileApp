import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    gap: 2,
    paddingHorizontal: '3%',
  },
  allDataContainer: {
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  }

});